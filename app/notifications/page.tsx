import NotificationsFeed from "@/components/users/NotificationsFeed";

const Notifications = () => {
  return (
    <section>
      <NotificationsFeed />
    </section>
  );
};

export default Notifications;
